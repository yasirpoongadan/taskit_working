import { ViewprojectPipe } from './viewproject.pipe';

describe('ViewprojectPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewprojectPipe();
    expect(pipe).toBeTruthy();
  });
});
